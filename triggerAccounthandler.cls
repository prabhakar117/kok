public class triggerAccounthandler {
    public static void claccount(List<Account> Newacclist, Map<Id, Account> NewAccMap, List<Account> oldacclist, Map<Id, Account> oldAccMap){
        for(Account acc : Newacclist){
            Account oldAccount = oldAccMap.get(acc.Id);
            
            
        }
    }

}